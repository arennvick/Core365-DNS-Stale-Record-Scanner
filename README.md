# Core365-DNS-Stale-Record-Scanner
A powerful PowerShell tool to scan, detect, and optionally remove stale DNS records left behind by decommissioned Domain Controllers — with a modern HTML dashboard report.
